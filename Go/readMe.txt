These files contains Go codes

Install on Linux:
$ sudo ap-get update[ENTER]
$ sudo ap-get upgrade[ENTER]
$ sudo apt-get install goland-go[ENTER]

Run:
$ go[ENTER]                 => Help
$ go run hello.go[ENTER]    => Run
